version: '3'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    environment:
      - POSTGRES_DB=highandtidy-postgres
      - POSTGRES_USER=highandtidy
      - POSTGRES_PASSWORD=Benzema1985
